Place AkiraExpanded.woff2 and AkiraExpanded.woff in this folder.

Vite is configured with publicDir = src/public, so these files will be served at:
- /fonts/AkiraExpanded.woff2
- /fonts/AkiraExpanded.woff
